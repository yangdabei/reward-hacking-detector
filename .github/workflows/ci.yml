# TODO [SWE EXERCISE 8 — CI/CD Pipeline]:
# Implement this GitHub Actions workflow. Guidelines:
#
# name: CI
#
# on:
#   push:
#     branches: [main]
#   pull_request:
#     branches: [main]
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.10"
#       - name: Cache pip dependencies
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
#       - name: Install dependencies
#         run: pip install -r requirements.txt
#       - name: Lint with ruff
#         run: ruff check src/ tests/
#       - name: Run tests
#         run: pytest tests/ -v --cov=src --cov-report=xml
#       - name: Build Docker image
#         run: docker build -t reward-hacking-detector .
#
# LEARN: GitHub Actions syntax, pip caching, running linters and tests in CI.

# PLACEHOLDER — implement the full workflow above
name: CI (placeholder)

on:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "TODO - implement CI workflow (SWE Exercise 8)"
